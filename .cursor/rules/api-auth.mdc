---
description: API endpoint authentication requirements
globs: src/routes/**/*.ts, src/index.ts
---

# API Authentication Rules

## Default: All Endpoints Admin-Only

All new API endpoints MUST require admin authentication by default. Use the `validateAuth` utility from `src/utils/auth.ts`.

## Authentication Pattern

Every route handler (except `/health`) must start with:

```typescript
import { validateAuth, unauthorizedResponse, forbiddenResponse } from '../utils/auth.js';

export async function handleYourEndpoint(request: Request): Promise<Response> {
    // Validate authentication
    const authResult = await validateAuth(request);
    if (!authResult.authenticated) {
        logger.warn('Unauthorized request', { error: authResult.error });
        return unauthorizedResponse(authResult.error || 'Unauthorized');
    }
    if (!authResult.isAdmin) {
        logger.warn('Non-admin request', { userId: authResult.userId });
        return forbiddenResponse('Admin access required');
    }

    // ... rest of handler
}
```

## Exceptions

Only the following endpoint should remain unprotected:
- `GET /health` - Health check for load balancers and monitoring

## Supported Auth Methods

The `validateAuth` function accepts:
1. **Password auth**: `X-Dashboard-Password` header (implicitly grants admin access)
2. **Bearer token**: `Authorization: Bearer <token>` header (Neon Auth JWT - checks `role` column in `neon_auth.user` table)

## When Adding New Routes

1. Create handler in `src/routes/`
2. Add auth check as first operation (both authentication AND admin check)
3. Register in `src/index.ts` passing the `request` object
